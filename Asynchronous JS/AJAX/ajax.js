//Explain how the query selector works
//QuerySelector is a method that allows us to select an element from the DOM using CSS selectors

//Explain how the query selector all works
//QuerySelectorAll is a method that allows us to select all elements from the DOM using CSS selectors

const xhr = new XMLHttpRequest();

xhr.open("GET", Url, yes);

xhr.setRequestHeader("Content-Type", "application/json");

xhr.onreadystatechange = function () {
   if(xhr.readyState === XMLHttpRequest.Done)
   {
        if(xhr.status === 200)
        {
            if(xhr.status === 200)
            {

            }
        }
   }
}

xhr.send(JSON.stringify(data));

xhr.responseText //returns the response as a string
xhr.responseType //returns the type of response
